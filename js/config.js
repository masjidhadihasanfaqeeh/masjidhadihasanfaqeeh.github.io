// Configuration for the website
// Change BASE_PATH if deploying to a subdirectory (e.g., '/masjidhadihasan/')
// For root deployment (e.g., username.github.io), use ''
const BASE_PATH = '';

// Helper function to get correct path
function getPath(path) {
    return BASE_PATH + path;
}
